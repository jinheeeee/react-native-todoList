import { ScrollView, Text, View } from "react-native";
import React, { Component } from "react";

const TodoList = ({}) => {
  return <ScrollView>dasfadf</ScrollView>;
};

export default TodoList;
